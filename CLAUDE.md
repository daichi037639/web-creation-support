# Webサイト制作支援サービス — CLAUDE.md

## プロジェクト概要

老舗企業・個人事業者など「良いものを持っているが魅力を伝えられていない」事業者が、
Webサイト制作とマーケティングの型を学びながら自走できるよう補助するサービス。
Next.js + Claude API + Supabase + Vercel で構成する。

## ディレクトリ構成

```
Webサイト制作支援/
├── CLAUDE.md
├── plans/
│   ├── 要件定義書.md
│   └── 検討事項ログ.md
└── app/               # Next.js プロジェクト
    ├── src/
    │   ├── app/       # App Router（ページ・APIルート）
    │   ├── components/# UIコンポーネント
    │   ├── lib/       # pure関数（Claude API・storage）
    │   └── types/     # TypeScript型定義
    └── .env.local     # ANTHROPIC_API_KEY を設定（要作成）
```

## やってはいけないこと

- plan の承認前に実装に着手しない
- 既存の設計・アーキテクチャと異なる方針を勝手に変更しない
- 大規模ライブラリを無断で追加しない（追加前に理由を説明し承認を得る）
- 要件定義書に定義されていない機能を勝手に実装しない

## 進め方のルール

- 実装前に必ず plan を確認・承認してから着手する
- PR は小さく。1機能 1PR を基本とする
- git は **merge**（rebase しない）
- 課題は GitHub issue で管理する
- 同じ指摘を2回したら、このファイルに追記する

## コーディングルール

- TypeScript を使い、型をきちんと定義する
- 関数は短く（理想10行以下、1画面超はNG）
- **DRY を意識する**：AIは放っておくとコードが重複する。共通処理はモジュール化する
- 副作用を分離し、pure関数に切り出してテストしやすくする
- コメントは「なぜそう書いたか」だけ書く

## テスト・品質

- unit test はできる限り書く。特に edge case / 境界値を多く書く
- lint / typecheck / unit test を必ず通してからPRを出す
- CI を整備して自動チェックする（GitHub Actions）
- Web UIの変更は **Playwright** で実際に動かして確認する

## AI との進め方

- 新しいライブラリ導入時は公式ドキュメントを参照させる
- 今日の日付が必要なときは `date` コマンドで取得する
- 2回指摘したことはこのファイルに追記する

## テキストで残す・git で管理する

- 課題 → GitHub issue
- 計画・設計判断 → `plans/` ディレクトリ または commit メッセージ・PR
- 検討した選択肢の履歴 → `plans/検討事項ログ.md`
- git で管理していないものは管理できていない、と考える

## サービス重要方針（要件定義書の要約）

- **対象**：Webやマーケティングが未経験の老舗企業・個人事業者
- **形式**：ウィザード（STEP 0〜7）で計画から公開まで一気通貫
  - STEP 0〜5：計画（事業・ターゲット・メッセージ・サイト構成をAIが引き出す）
  - STEP 6：サイト生成（自然言語でAIがコードを生成。iframeでプレビュー）
  - STEP 7：公開（ユーザー自身のGitHub + Vercelアカウントへデプロイ）
- **生成コード**：機能の複雑さで静的HTML か Next.js を自動判定・提案
- **認証**：MVP は認証なし。後付けできる設計にしておく（Supabase）

## 参照ドキュメント

- [Vibe Coding ガイド](../vibe_coding.md)
- [要件定義書](./plans/要件定義書.md)
- [検討事項ログ](./plans/検討事項ログ.md)
